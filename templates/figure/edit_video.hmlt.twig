{# templates/figure/edit_video.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Modifier la vidéo{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <h1>Modifier la vidéo</h1>

            {{ form_start(form) }}
            {# {{ form_widget(form) }} #}

            <button type="submit" class="btn btn-primary">Enregistrer les modifications</button>
            <a href="{{ path('app_figure_show', {'slug': figure.slug}) }}" class="btn btn-secondary">Annuler</a>

            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
